# Custom Sidebar Toggle (Streamlit)

This project uses a **custom sidebar component** to bypass the limitations of
Streamlit’s built-in sidebar and provide full control over open/close behavior.

You get:

- ✅ Green **Open** button in the **top-right corner** of the app  
- ✅ Purple **Close** button in the **top-right corner of the sidebar**  
- ✅ Smooth, reliable toggle  
- ✅ No DOM hacks on Streamlit’s native sidebar  
- ✅ Works across versions without layout breakage  

---

## 🔧 Requirements

Install the required Python packages:

```bash
pip install -r requirements.txt

